import 'package:flutter/material.dart';

class MyWorksCard extends StatelessWidget {
  const MyWorksCard({super.key, required this.yourData});

  final String yourData;

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
